version: '3.8'

services:
  app:
    env_file:
      - .env
    build:
      context: .
      dockerfile: ./Dockerfile
      target: dev
    ports:
      - '3000:3000'
    volumes:
      - .:/usr/app:delegated
      - /usr/app/node_modules
