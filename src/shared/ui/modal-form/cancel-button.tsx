import React from 'react';

import { Button, ButtonProps } from '@chakra-ui/react';

const SubmitButton: React.FC<ButtonProps> = (props) => <Button {...props}>Cancel</Button>;

export default SubmitButton;
