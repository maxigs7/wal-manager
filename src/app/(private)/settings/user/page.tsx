import { Title } from '@/layout/settings';

export const revalidate = 0;
export const metadata = {
  title: 'Usuario',
};

const Page = () => {
  return <Title>Usuario</Title>;
};

export default Page;
